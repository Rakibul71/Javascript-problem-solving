// find landscape mode
// if width>height= landscope

function isLandscape(width, height) {
  return width > height ? "landscape" : "input right value";
}

const value = isLandscape(60, 33);
console.log(value);
